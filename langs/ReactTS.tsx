// TSX is an extension of TypeScript commonly used with React for writing type-safe JSX code.

import React from 'react';

const HelloWorld: React.FC = () => {
  return <h1>Hello, World!</h1>;
}

export default HelloWorld;